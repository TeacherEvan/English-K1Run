# Configure npm for better handling of optional dependencies
# This helps with platform-specific binary packages like @rollup/rollup-android-arm64

# Include optional dependencies by default (npm 9+ syntax)
include=optional

# Set longer timeout for slower connections (useful for Termux/mobile environments)
timeout=300000

# Prefer exact versions to avoid dependency resolution issues
prefer-exact=true

# Disable strict engine checking (helpful for alternative Node.js environments)
engine-strict=false

# Enable audit level info (for security while allowing flexibility)
audit-level=moderate