# Refactor plan — `src/components/ui/language-selector.tsx` in `TeacherEvan/English-K1Run`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-11T05:17:18.934069+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/English-K1Run` |
| path | `src/components/ui/language-selector.tsx` |
| size | 9.3 KB (9,509 bytes) |
| lines | 196 |
| generated | 2026-09-11T05:17:18.934086+07:00 |

### Top imports (sample)
  - `import { useSettings } from '@/context/settings-context'`
  - `import { useLanguage } from '@/hooks/use-language'`
  - `import { LANGUAGE_OPTIONS, type SupportedLanguage } from '@/lib/constants/language-config'`
  - `import { cn } from '@/lib/utils'`
  - `import { SVG_NAMESPACE } from './svg-constants'`
  - `import * as SelectPrimitive from '@radix-ui/react-select'`
  - `import { useTranslation } from 'react-i18next'`
  - `import React, { useState } from 'react'`

## Structural analysis

**Top-level consts:**
- `Check` (L17)
- `ChevronDown` (L22)

**Render branches:** 1 `return (` blocks at L75

**Top-referenced imports:**
- `cn` (8 refs)
- `LANGUAGE_OPTIONS` (3 refs)
- `SVG_NAMESPACE` (3 refs)
- `useSettings` (2 refs)
- `useLanguage` (2 refs)
- `useTranslation` (2 refs)
- `useState` (2 refs)

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `English-K1Run`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Confirm `cn` (8 refs) is the right shared utility
- **Target:** `cn` is the most-referenced import in this file (8 uses).
- **Action:** verify whether other dashboard pages also import it; if so, ensure it lives
  in a shared `lib/` or `@/components/ui/` and not in a feature-local path.
- **Acceptance:** document the canonical home for `cn` in `docs/`.
- **Validation:** `rg -l 'cn' src/` — should match ≤3 call sites in
  feature code, more in shared.
- **Evidence:** call-site map.

### OBJ-002 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `src/components/ui/language-selector.tsx` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `src/components/ui/language-selector.tsx`.
- **Validation:** `rg -n '^import' src/components/ui/language-selector.tsx` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-003 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `src/components/ui/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-004 — Hardening pass 4
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-005 — Hardening pass 5
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-006 — Hardening pass 6
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `src/components/ui/language-selector.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `src/components/ui/language-selector.tsx` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `English-K1Run`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-11-bigfile-English-K1Run-language-selector.tsx.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.


## REVIEW 2026-09-11T05:46:34.910361+07:00

**Verdict:** `NEEDS-REVISION`

**Structural check:** objectives=12 file_header=✓ imports=✓ why=✓ dod=✓ security=✓

**Gaps:**
1. **OBJ-004–OBJ-012 are identical filler.** Nine "hardening passes" with verbatim-identical text, no line anchors, no symbol anchors, no distinct targets. This directly contradicts the plan's own claim of being "file-aware … NOT a generic N-slice filler." A real hardening pass would name a specific loose type or prop-drilling site (e.g., `SelectPrimitive.ItemProps`, `SVG_NAMESPACE` usage, `SupportedLanguage` union). These nine objectives contribute zero actionable work.
2. **DoD section is truncated.** The plan cuts off mid-sentence: *"preserving behav"* — no completion, no checklist for the split/shrink criteria, no rollback conditions. The DoD is not a DoD.
3. **Structural checker reports `objectives=0`** despite the plan listing 12 numbered objectives. The checker also reports `has_header=None`, `has_imports=None`, `has_why=None`, `has_dod=None`, `has_security=None`. Either the plan format doesn't match what the checker expects, or the objectives aren't parseable. Either way, the plan fails its own structural gate.
4. **OBJ-001 is under-scoped.** It asks to "verify whether other dashboard pages also import `cn`" but the acceptance is only *"document the canonical home for `cn` in `docs/`"* — no action to actually move or consolidate anything. The validation (`rg -l 'cn' src/`) is also mis-targeted: `cn` is a function name, not a literal string, so `rg` will return far more than 3 call sites.
