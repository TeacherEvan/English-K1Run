/**
 * Shared SVG constants for UI icon components.
 *
 * The `http://www.w3.org/2000/svg` namespace is repeated across inline
 * Lucide-style SVGs; hoisting it here keeps every consumer in sync and
 * makes the string a single audit/localization point.
 */

export const SVG_NAMESPACE = 'http://www.w3.org/2000/svg'
